# Mac Media Player

A native macOS application for playing audio and video media stored on the user's Mac.

## Language

**Local Media**:
An audio or video file stored on the user's local filesystem and selected for playback.
_Avoid_: Stream, online media, remote media

**本地媒体引用**:
对用户明确选择的某个本地媒体的共享身份和定位关系；身份优先跟随实际文件而非路径文本，多个播放列表条目可以引用它，重定位后这些条目一并恢复。
_Avoid_: 播放列表条目、媒体资料库记录

**文件缺失**:
本地媒体引用暂时无法定位或访问其目标文件的状态；引用及相关播放列表条目仍然保留，自动播放会跳过它，手动选择则进入重定位或移除流程。
_Avoid_: 已删除条目、无效播放列表

**文件夹导入**:
将所选文件夹及其子文件夹中受支持的本地媒体一次性展开为播放列表条目的操作；默认跳过目标播放列表中已存在的媒体，只有显式选择时才重复导入，且不建立持续监视。
_Avoid_: 文件夹扫描、媒体资料库导入

**Playlist**:
具有稳定身份和唯一名称的播放列表条目有序集合；名称可变且忽略大小写后唯一，它只引用本地媒体，不拥有或管理磁盘上的源文件。
_Avoid_: 媒体资料库、文件夹扫描、磁盘文件容器

**播放列表条目**:
播放列表中具有独立身份和顺序位置的一次本地媒体引用；同一本地媒体可以通过不同条目重复出现。
_Avoid_: 文件、媒体本身

**续播位置**:
某个播放列表条目播放超过 10 秒后中断时的媒体时间点；同一本地媒体的不同播放列表条目各自拥有独立的续播位置。
_Avoid_: 文件进度、全局进度

**已播完**:
播放列表条目的剩余时间不超过 30 秒与总时长 5% 中较小者时形成的完成状态；再次打开该条目时从头播放。
_Avoid_: 片尾续播位置、自动删除

**条目播放偏好**:
播放列表条目最近一次成功使用的音轨、内嵌字幕和外部字幕选择；当原选择不可用时，它失效并由默认选择规则接管。
_Avoid_: 文件偏好、全局轨道设置

**外部字幕引用**:
对用户选择的外部字幕文件的共享身份和定位关系；播放列表条目独立保存是否使用它，引用缺失不阻止媒体播放。
_Avoid_: 媒体引用、内嵌字幕轨道

**默认轨道规则**:
应用根据用户的首选音频语言、首选字幕语言和字幕自动策略选择音轨与字幕的全局回退规则；条目播放偏好具有更高优先级。
_Avoid_: 条目播放偏好、固定第一轨

**播放顺序**:
播放列表决定如何选择下一条目的持久化设置，取值为顺序或随机；它与重复方式相互独立。
_Avoid_: 播放模式、重复方式

**重复方式**:
播放列表决定播放到边界后如何继续的持久化设置，取值为不重复、列表循环或单条循环；它与播放顺序相互独立。
_Avoid_: 播放模式、播放顺序

**随机轮次**:
播放列表在随机顺序下生成并持久保存的一轮条目顺序和已播放历史；一轮内每个条目最多被自动选择一次。
_Avoid_: 每次切歌重新随机、列表顺序

**播放速度**:
播放列表中所有条目共享的持久化播放倍率；每个播放列表独立保存，新建播放列表默认为正常速度。
_Avoid_: 条目速度、全局速度

**播放器音量**:
整个应用共享并持久化的播放器增益和独立静音状态；它不随播放列表切换，也不修改系统音量。
_Avoid_: 播放列表音量、系统音量

**播放上下文**:
应用最近活动的命名播放列表；每个播放列表分别保存自己的当前条目，应用重启时恢复二者但保持暂停。
_Avoid_: 自动续播、临时列表状态

**正在播放的播放列表**:
当前媒体及后续自动推进所依据的播放列表；它决定播放顺序、重复方式和播放速度。
_Avoid_: 正在浏览的播放列表、临时队列

**正在浏览的播放列表**:
用户当前查看或编辑的播放列表；切换它不会中断媒体，也不会改变正在播放的播放列表。
_Avoid_: 正在播放的播放列表

**脱离列表的播放项**:
正在播放的条目被移除后，仅维持到当前播放结束、停止或切换的临时播放状态；它不再属于播放列表，也不再持久保存条目状态。
_Avoid_: 隐藏条目、自动重新插入

**正在播放列表**:
用户直接打开一个或多个文件时形成的临时、未命名且不持久化的有序播放集合；每次“打开”都会替换它，它不会隐式修改任何播放列表。
_Avoid_: 队列、临时播放列表

**Media Library**:
An automatically scanned and indexed catalogue of media available on the user's Mac. It is not part of the MVP.
_Avoid_: Playlist
