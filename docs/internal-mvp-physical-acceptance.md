# 离线内部 MVP 候选物理机验收结果

## 结论

- 结果：`FAIL`
- 阻断原因：4K HEVC 代表样本未进入 VideoToolbox 硬件解码路径，应用明确提示已降级为软件解码。Issue #17 要求核心 4K 场景通过硬件解码质量与资源门槛，因此该候选不能获得 M5 离线内部 MVP 资格。
- 范围：仅离线内部 MVP 候选；不是公开发布或 App Store 资格。
- 停止规则：出现上述硬阻断后，不再消耗 30 分钟热稳态、QuickTime 能耗对照及完整语料矩阵的执行时间。未执行项同样阻止 `PASS`。

## 候选身份

- 提交：`ac87a35d422a173321827a16f7385f7297b5a5c9`
- 构建时间（UTC）：`20260808T115608Z`
- 可执行文件 SHA-256：`484ae290d27a28e90608e1b9baecef2d9d69447c59bb3cbcadd610e4510936b8`
- 动态依赖闭包 SHA-256：`86ee7033146f56417b9d1eda1e2b299e90eaf051fd1ec6249fb1e5cc6521a534`
- 架构与最低系统：`arm64 + x86_64`，macOS 14.0
- 播放引擎：mpv `v0.41.0`，FFmpeg `n8.1.2`
- 自动门槛：Swift 合约 `93/93`、Xcode 测试 `39/39`、App/真实引擎合约、全新与已有用户数据、签名启动均为 `PASS`。

## 环境与隐私边界

- 设备：Apple M5 MacBook Pro，24 GB 内存（不记录序列号、UUID 或账户信息）
- 系统：macOS 26.6（25G72）
- 语言：简体中文与英文；外观：浅色、深色、增强对比度；辅助功能：旁白、减弱动态效果
- 语料证据只记录可公开复现的生成样本名称、媒体属性和 SHA-256，不记录私人媒体路径或内容。
- 验收结束后已恢复深色外观，关闭旁白、增强对比度和减弱动态效果，并移除候选 App 的临时语言覆盖。

## 场景记录

| 场景 | 初始状态 | 操作 | 期望 | 恢复路径 | 证据与结果 |
| --- | --- | --- | --- | --- | --- |
| 已有数据与只读书签恢复 | 已签名候选首次启动，存在上轮保存的 Playlist | 启动精确候选并等待恢复 | 恢复 Playlist、条目和暂停状态，不重新授权即可访问仍有效的书签 | 权限失效时应进入重新授权流程 | 恢复 `验收-079aa739` 与对应媒体，`PASS`；权限丢失分支未执行，整体为 `PARTIAL` |
| 720p H.264/AAC 播放与快捷键 | 已恢复条目、暂停、中文、深色 | 空格播放、右箭头跳转、`P` 收起侧栏、`Control-Command-F` 全屏、`Escape` 退出 | 播放/跳转/侧栏/全屏状态正确且焦点不丢失 | 停止或退出全屏 | 全部动作生效；硬解初始化失败后按产品策略降级到 1080p 及以下软件解码，`PASS`（仅该代表场景） |
| 4K HEVC 核心硬件路径 | 中文、深色、播放器可用 | 通过系统打开面板载入 3840×2160、30 fps、HEVC/AAC、6 秒生成样本 | 使用 VideoToolbox，并满足首帧、丢帧、同步、内存与热稳态门槛 | 允许明确的软件解码降级，但不能据此通过核心硬件门槛 | 样本 SHA-256 `e2ea8ddb4ab6899b068f3da70355d78f3779d3977478cfd5ed5a7eeb983b1ba5`；应用提示“4K 已明确降级为软件解码”，`FAIL` |
| 24 小时音频寻址 | 英文、深色、ALAC 生成样本未加载 | 打开样本，从开头点击寻址至中段与末端 | 显示 24 小时时长；中段与末端寻址稳定，无溢出或崩溃 | 停止播放或重新打开 | 样本时长 `86400` 秒，SHA-256 `db86ca29291cea1b899eda7f24124e380d1249d3b53b9fb86308488a37e54e45`；显示 `24:00:00`，到达 `12:43:09` 与 `24:00:00`，`PASS` |
| 损坏媒体分类与恢复 | 英文、深色、无有效媒体在播放 | 打开伪装为 MP4 的无效内容，执行重试与跳过 | 给出稳定领域错误并显示恢复动作，不崩溃、不删除源文件 | 重试、在 Finder 中显示、仅移除列表条目、跳过 | 分类为“Media content is corrupted”；四个动作全部可见，重试后错误稳定、跳过后恢复面板消失，`PASS` |
| 中英文与不支持语言回退 | 中文启动并完成播放场景；随后分别用英文与不支持的法文重启 | 检查菜单、播放控制、Playlist、轨道、字幕与错误文本 | 核心路径完整本地化；不支持语言回退到英文 | 删除 App 语言覆盖并重启 | 中文与英文控件语义完整；法文偏好下菜单与控件回退为英文，`PASS` |
| 浅色、深色与增强对比度 | 英文、暂停、侧栏可见 | 依次切换浅色、深色、增强对比度并检查关键控件与状态 | 文本、按钮、选中态及播放状态可辨识，不只依赖颜色 | 恢复原始深色与默认对比度 | 三种设置下控件和状态语义完整、可辨识，`PASS` |
| 减弱动态效果 | 英文、暂停、侧栏展开 | 启用减弱动态效果，收起侧栏，进入并退出全屏 | 状态直接切换且不重置播放/焦点 | 恢复原始设置 | 侧栏变为 `Collapsed`，全屏进入与退出成功，`PASS` |
| 旁白与键盘焦点 | 英文、暂停、旁白关闭 | 开启旁白，进入候选并按 Tab | 焦点进入播放器且每个核心控件有名称、值和帮助文本；完整完成打开、播放、Playlist 与错误恢复 | 关闭旁白 | 焦点进入“Back 10 seconds”，完整 AX 树可读；未在旁白下跑完全部核心路径，`PARTIAL` |
| 物理媒体键 | 英文、候选运行 | 尝试发送播放/暂停媒体键 | 播放状态切换，并验证上一首/下一首 | 应用内按钮与普通键盘快捷键 | 当前自动化输入层不支持 `XF86AudioPlay`，未以真实硬件完成，`NOT RUN` |
| 完整格式/轨道/字幕/VFR/长 GOP/失败语料矩阵 | 已生成 WebM、VFR、长 GOP、多音频格式、SRT/ASS/SSA 与损坏样本 | 在发现 4K 硬阻断前仅执行代表子集 | 按 Issue #17 逐项记录首帧、跳转、轨道、字幕、同步与恢复结果 | 失败即停止并保留证据 | 代表子集有结果；完整矩阵未继续，`NOT RUN` |
| 30 分钟热稳态、内存与 QuickTime 对照 | 4K 硬件路径先决条件不成立 | 未执行 | 记录丢帧、同步、内存趋势、温度/降频及 QuickTime 能耗对照，并按重复规则复测 | 修复硬件路径后重跑 | `NOT RUN` |

## 测试稳定性修复

物理验收前的重复运行暴露了 `performsPlaybackFailureRecoveryActions` 的既有竞态：测试只等待 `.failed` 状态，尚未等待公开的 `playbackFailureNotice` 发布恢复动作就调用重试、跳过或移除。测试现改为在 Issue #17 预先约定的 `PlaybackCoordinator` 缝隙上等待对应公开恢复动作，不改变产品行为。修复后的聚焦测试重复运行 `100/100` 通过，随后正式候选构建与自动门槛全部通过。

## 重新取得资格所需动作

1. 修复或明确定位 M5 上 VideoToolbox 初始化失败，确认 4K 核心样本实际进入硬件解码路径。
2. 使用真实键盘完成播放/暂停、上一首和下一首媒体键验收。
3. 在旁白下跑完打开媒体、播放控制、Playlist 和错误恢复全路径，并补做权限丢失恢复。
4. 从同一修复提交生成新的签名候选，重跑完整语料矩阵、30 分钟热稳态、内存趋势与 QuickTime 对照；所有阻断项通过后才可记录 `PASS`。

首次公开发布仍由独立门槛负责，包括 M1 与 2018 Intel 资格、分发签名、App Store 导出/preflight、法务和编解码器专利审查。
